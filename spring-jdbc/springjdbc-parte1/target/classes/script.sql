--DELETA A TABELA PESSOA CASO A TABELA EXISTA
DROP TABLE IF EXISTS PESSOA;
--CRIA A TABELA PESSOA
CREATE TABLE PESSOA(
       ID INT AUTO_INCREMENT PRIMARY KEY NOT NULL, 
       NOME VARCHAR(500), 
       EMAIL VARCHAR(500)
);
--DELETA A TABELA TELEFONE CASO EXISTA
DROP TABLE IF EXISTS TELEFONE;
--CRIA A TABELA TELEFONE
CREATE TABLE TELEFONE(
       ID INT AUTO_INCREMENT PRIMARY KEY NOT NULL, 
       DDD VARCHAR(2), 
       NUMERO VARCHAR(9), 
       TIPO VARCHAR(10) ,
       ID_PESSOA INT , 
FOREIGN KEY(ID_PESSOA) REFERENCES PESSOA(ID));

CREATE ALIAS IF NOT EXISTS GET_PRIME_NUMBERS FOR "br.com.javamagazine.procedure.H2StoredProcedures.getPrimes"


